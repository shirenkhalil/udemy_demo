import 'package:flutter/material.dart';

class My_Learning extends StatelessWidget {
  const My_Learning({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Text('My_Learning'),
    );
  }
}